# Frontend API path used by the browser.
# In development this should usually stay as /api so Vite can proxy requests.
VITE_API_BASE_URL=/api

# Dev-only proxy target for Vite.
VITE_DEV_API_TARGET=http://127.0.0.1:8000
